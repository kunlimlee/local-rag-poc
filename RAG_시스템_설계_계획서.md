# 사내 문서 RAG 구축 PoC 계획서

> **목적**: 기업 문서를 사내 PC에 보관한 채, 질의 시 최소 발췌만 외부 LLM으로 전송하는 RAG 챗봇이 실제로 동작함을 실증한다.
> **성격**: 제품 개발이 아니라 **개념 증명(PoC)**.
> **LLM**: Gemini 3.5 Flash (답변 품질·속도 우선 판단에 따름)

---

## 1. 검증 질문 3가지

| # | 검증 질문 | 확인 방법 | 성공 기준 |
|---|---|---|---|
| Q1 | 고객사 PC에서 외부 API 호출이 되는가? | 사내망에서 API 연결 테스트 | 프록시/방화벽 통과 확인 |
| Q2 | 답변 속도가 실용적인가? | 답변 생성 시간 실측 | **답변 완료 10초 이내** |
| Q3 | 사내 문서로 정확한 답변이 나오는가? | 질문 5개 수동 확인 | **5개 중 4개 이상 출처 정확** |

> **Q1이 최대 리스크다.** 기업 PC는 프록시·방화벽으로 외부 API를 막는 경우가 흔하다.
> 여기서 막히면 나머지가 무의미하므로 0단계에서 먼저 확인한다.

---

## 2. 데이터 경계 (컨설팅 핵심 자료)

고객사 보안팀에 제시하는 표. **정확한 표현을 쓰는 것이 중요하다.**

| 구성요소 | 실행 위치 | 외부 전송 |
|---|---|---|
| 원본 문서 (전체) | 사내 PC 디스크 | **없음** |
| 임베딩 모델 (bge-m3) | 사내 PC | **없음** |
| 벡터 DB (ChromaDB) | 사내 PC 폴더 | **없음** |
| LLM (Gemini 3.5 Flash) | Google 클라우드 | **질문 + 관련 청크 5개** |

### 고객에게 쓸 표현

> ✅ "전체 문서는 사내 PC에만 보관됩니다. 질문 시 관련된 일부 발췌만 전송되며,
>    전송분은 학습에 사용되지 않습니다(유료 티어 기준)."
>
> ❌ "데이터가 외부로 나가지 않습니다" — **사실이 아니므로 사용 금지.**

### ⚠️ 라이브러리 기본값 차단 목록 (2026-09-07 실제 발견)

**주요 라이브러리는 기본값이 "외부 전송 켜짐"이다.** 그대로 두면 "데이터가 나가지 않는다"는
설명이 거짓이 된다. 고객사 보안팀이 패킷을 뜯어보면 바로 드러난다.

| 항목 | 기본값 | 조치 | 적용 위치 |
|---|---|---|---|
| ChromaDB 사용 통계 | **전송 켜짐** (PostHog) | `Settings(anonymized_telemetry=False)` | `rag.py`, `index.py` |
| Streamlit 사용 통계 | **전송 켜짐** | `gatherUsageStats = false` | `.streamlit/config.toml` |
| Streamlit 네트워크 바인딩 | **0.0.0.0 (전체 개방)** | `address = "localhost"` | `.streamlit/config.toml` |

> 세 번째가 가장 위험하다. 기본 설정으로 띄우면 **같은 망의 아무나 사내 문서 챗봇에
> 접속**할 수 있다. 실제로 기동 로그에 `External URL: http://118.34.224.17:8501` 이 찍혔다.
>
> 검증 방법 — 다른 PC나 휴대폰에서 `http://<시연PC IP>:8501` 접속을 시도해 **연결이
> 거부되는지** 확인한다. 고객 앞에서 이걸 보여주면 설득력이 크다.

**교훈**: 새 라이브러리를 도입할 때마다 텔레메트리·네트워크 기본값을 확인할 것.

### 임베딩을 로컬로 유지하는 이유 (설계상 가장 중요한 결정)

| 방식 | 외부로 나가는 양 |
|---|---|
| 임베딩도 클라우드 API 사용 | 인덱싱 시점에 **전체 문서 전량** |
| **임베딩 로컬 (본 설계)** | 질문당 **청크 5개만** |

→ 문서가 1,000개여도 실제 노출은 질문당 5조각. **이 구조를 절대 바꾸지 말 것.**

---

## 3. ⚠️ 무료 티어 사용 기준 (필독)

Google 공식 약관(Gemini API Terms) 확인 결과:

**무료 티어(Unpaid Services)**
- "Google uses the content you submit... to provide, improve, and develop Google products and services." → **학습에 사용됨**
- "Human reviewers may read, annotate, and process your API input and output." → **구글 직원이 읽을 수 있음**
- "**Do not submit sensitive, confidential, or personal information to the Unpaid Services.**" → **기밀정보 제출 금지 명시**

**유료 티어(Paid Services)**
- "Google doesn't use your prompts... or responses to improve our products." → **학습에 사용 안 함**
- 로그는 정책 위반 탐지 목적으로 제한적 기간만 보관

### 적용 기준

| 상황 | 티어 | 이유 |
|---|---|---|
| 개발·기능 테스트 (**샘플/더미 문서만**) | 무료 | 유출될 정보가 없음 |
| **고객사 실제 문서 투입 시점** | **유료 필수** | 구글 약관 위반 + 데이터 거버넌스 사고 |
| 고객 앞 시연 | **유료 권장** | 무료는 분당 요청 제한으로 시연 중 끊길 수 있음 |

> Flash 계열은 유료여도 단가가 매우 낮다. PoC 사용량에서 실제 청구액은 미미하다.
> **결제 수단 등록만으로 위 리스크가 전부 해소된다.**

> 📌 무료 티어 제한(RPM/RPD)은 계정·시점별로 다르므로 [Google AI Studio](https://aistudio.google.com/)에서 직접 확인할 것. (블로그 인용값은 신뢰하지 말 것)

---

## 4. 고정 기술 스택 (선택지 없음)

| 항목 | 선택 | 고정 이유 |
|---|---|---|
| 문서 로딩 | LangChain 로더 | PDF/DOCX/TXT를 한 코드로 처리 |
| 청킹 | `RecursiveCharacterTextSplitter` (1000자 / 200 겹침) | 무난한 기본값 |
| 임베딩 | `BAAI/bge-m3` (**로컬**) | 문서 전량 외부 전송 방지 |
| 벡터 DB | ChromaDB (파일 기반) | 서버 설치 불필요 |
| 검색 | 코사인 유사도 Top-5 | 가장 단순 |
| LLM | **`gemini-3.5-flash-lite`** (thinking=LOW) | **실측 최속(3.2초) + 정답**. 아래 근거 참조 |
| UI | Streamlit | 시연용으로 충분 |

#### LLM 선정 근거 (2026-09-07 실측)

동일 프롬프트(문서 발췌 + 2단계 추론 질문)로 측정한 결과:

| 모델 | thinking | 첫 글자 | 완료 | 정답 |
|---|---|---|---|---|
| **`gemini-3.5-flash-lite`** | LOW | **0.86초** | **3.22초** | O |
| `gemini-3.5-flash` | MINIMAL | 7.97초 | 9.00초 | O |
| `gemini-3.5-flash` | LOW | 10.87초 | 11.52초 | O |
| `gemini-3.5-flash` | MEDIUM | 14.75초 | 15.64초 | O |
| `gemini-3.6-flash` | LOW | 25.70초 | 26.83초 | O |
| `gemini-3.7-flash` | LOW | — | 503 과부하 | — |
| `gemini-3.8-flash` | 조절 불가 | — | 503 과부하 | — |

**최신 모델을 쓰면 안 되는 이유**: `3.7`/`3.8-flash`는 호출 시 **503(고부하)** 이 발생했다.
고객 시연 중 503은 치명적이다. 또 `3.8-flash`는 thinking 수준 조절이 거부(400)되어
지연을 낮출 방법이 없다. `2.5-flash`는 신규 사용자 제공 종료(404).

> ⚠️ **정확도는 1회 측정으로 판정하지 말 것.** 1차 측정에서 `3.5-flash/MINIMAL`이
> 오답(16일)을, 2차에서 동일 설정이 정답(17일)을 냈다. LLM은 같은 입력에도 결과가 흔들린다.
> 위 표의 "정답 O"는 각 1회 결과일 뿐이며, 품질 판정은 Q3 검증에서 반복 측정으로 한다.

**제외한 것**: 리랭킹 · BM25 하이브리드 · 의미론적 청킹 · 설정 UI
→ 검증 질문 3개와 무관. Q3 품질 부족이 **측정된 뒤에** 검토한다.

### 파일 구조

```
Anti_Claude_RAG/
├── .env               # GEMINI_API_KEY  ← .gitignore 등록됨
├── .venv/             # 가상환경
├── loader.py          # 공용 문서 로더 (pypdf/docx2txt 직접 호출)
├── setup_key.py       # API 키를 .env 에 저장
│
├── step0_api_test.py    # 0단계: API 연결 확인          ✅ 완료
├── step0_speed_test.py  # 0단계: 모델 속도·정확도 실측  ✅ 완료
├── step1_load_test.py   # 1단계: 문서 텍스트 추출 확인  ✅ 완료
│
├── index.py           # 2단계: 청크 → 임베딩 → ChromaDB
├── ask.py             # 4단계: 질문 → 검색 → Gemini → 답변
├── app.py             # 5단계: Streamlit UI
│
├── docs/              # 대상 문서 폴더 (.gitignore 등록됨)
└── vector_db/         # ChromaDB 저장 폴더
```

> 단계별 `stepN_*.py`는 **검증용 스크립트**이고, `index.py`/`ask.py`/`app.py`가
> 최종 산출물이다. 검증 스크립트를 남겨두는 이유는 고객사 PC에서 문제가 생겼을 때
> 어느 단계가 깨졌는지 하나씩 짚어볼 수 있기 때문이다.

### requirements.txt (2026-09-07 설치·검증 완료)

```
google-genai              # Gemini API
python-dotenv             # .env 에서 API 키 읽기
pypdf                     # PDF 텍스트 추출
docx2txt                  # DOCX 텍스트 추출
langchain-text-splitters  # 청킹 (문장 경계 보존)
sentence-transformers     # 로컬 임베딩 (2단계)
chromadb                  # 로컬 벡터 DB (2단계)
streamlit                 # UI (5단계)
```

**LangChain을 쓰지 않는다** (`langchain-text-splitters` 제외).
`langchain-community`는 **지원 종료(sunset) 예정**이며, 그 문서 로더들은 결국
`pypdf`/`docx2txt`를 감싼 껍데기다. 직접 호출하면 의존성 4개(`langchain-community`,
`langchain-classic`, `langchain-core`, `langchain-protocol`)가 사라지고
폐쇄망 반입 용량도 줄어든다. 청킹만 예외로 남긴 이유는 문장 경계를 지키며 자르는
로직이 직접 구현 시 실수하기 쉬운 부분이기 때문이다.

**환경 검증 결과**: Python 3.14.6에서 위 패키지 모두 설치 확인.
`torch` 2.14.0이 cp314 휠을 제공하므로 2단계 로컬 임베딩도 동작 가능.

---

## 5. 사전 준비

Ollama 대비 반입 부담이 크게 줄었다. 단, **임베딩 모델은 여전히 로컬**이므로 사전 반입이 필요하다.

| 항목 | 준비 방법 | 용량 | 관리자 권한 |
|---|---|---|---|
| **VC++ 2015-2022 재배포 패키지 (x64)** | [aka.ms/vs/17/release/vc_redist.x64.exe](https://aka.ms/vs/17/release/vc_redist.x64.exe) | 약 25MB | **필요** |
| Python 패키지 | 폐쇄망이면 `pip download -r requirements.txt -d ./wheels` | 수백 MB | 불필요 |
| 임베딩 모델 | HuggingFace 사전 다운로드 후 `~/.cache/huggingface` 복사 | **bge-m3: 4.3GB**<br>ko-sroberta: 423MB | 불필요 |
| Gemini API 키 | Google AI Studio에서 발급 | - | 불필요 |

> ⚠️ **VC++ 재배포 패키지는 사전 확인 필수 항목이다.** (2026-09-07 개발 PC에서 실제 발생)
> `torch`(로컬 임베딩)는 이 패키지에 포함된 `vcruntime140_1.dll`을 요구한다. 없으면
> `OSError: [WinError 1114] ... c10.dll` 로 실패한다. 일반 사무용 PC에는 없을 수 있고,
> 설치에 **관리자 권한**이 필요하므로 방화벽 승인과 함께 **IT팀에 미리 요청**해야 한다.
>
> 확인 방법 — `C:\Windows\System32\vcruntime140_1.dll` 파일이 있는지 본다. 없으면 설치 필요.

- 오프라인 설치: `pip install --no-index --find-links=./wheels -r requirements.txt`
- **API 키는 `.env`에 두고 절대 코드에 하드코딩하지 말 것.** 고객사 화면 공유 중 노출 위험.

---

## 6. 실행 단계

각 단계는 **독립 실행 가능**하며, 끝에 눈으로 확인할 산출물이 있다.
**리스크가 큰 순서로 배치했다.**

### 0단계 — 사내망에서 API 연결 확인 ⚠️ 최우선

| 항목 | 내용 |
|---|---|
| 목표 | 고객사 네트워크에서 Gemini API가 호출되는가 |
| 실행 | API 키 발급 → 사내 PC에서 "안녕" 한 마디 호출 |
| 확인 | 응답이 오는가 / 응답까지 몇 초인가 |
| 실패 시 | 프록시 설정 필요, 또는 **방화벽 도메인 허용 신청** (승인에 수일 소요 가능 → 일정 반영) |

> 기업 PC에서 가장 흔한 실패 지점이다. **고객사 방문 전에 미리 확인할 것.**

### 1단계 — 문서 텍스트 추출 확인

| 항목 | 내용 |
|---|---|
| 목표 | 고객사 실제 문서 형식에서 텍스트가 제대로 나오는가 |
| 실행 | 대표 문서 5개를 `docs/`에 넣고 로더로 읽어 앞부분 출력 |
| 확인 | 한글 안 깨짐, 표·머리글 뒤섞임 정도 육안 확인 |
| 실패 시 | **스캔 PDF(이미지)면 텍스트가 안 나온다** → OCR 필요 여부 결정 |

### 2단계 — 인덱싱

| 항목 | 내용 |
|---|---|
| 목표 | 청킹 → 임베딩 → ChromaDB 저장 |
| 실행 | `python index.py` |
| 확인 | 청크 개수 출력, `vector_db/` 폴더 생성 |
| 기록 | **문서 N개 인덱싱 소요 시간** (전체 문서량 환산 근거) |
| 참고 | 최초 1회 bge-m3 로딩에 시간이 걸린다. 이후로는 빨라진다 |

### 3단계 — 검색만 테스트 (LLM 없이)

| 항목 | 내용 |
|---|---|
| 목표 | 질문에 관련된 청크가 실제로 검색되는가 |
| 실행 | 질문 입력 → Top-5 청크와 파일명 출력 |
| 확인 | 사람이 봤을 때 "이 청크면 답할 수 있겠다" 싶은가 |
| 실패 시 | 여기서 안 나오면 LLM을 붙여도 답이 안 나온다. 청크 크기부터 조정 |

> **검색과 생성을 분리해 확인하는 것이 핵심.** 답변이 이상할 때 원인이 구분된다.
> 이 단계는 **외부 전송이 전혀 없다.**

### 4단계 — Gemini 연결 (RAG 완성)

| 항목 | 내용 |
|---|---|
| 목표 | 검색 청크 + 질문 → Gemini → 답변 |
| 실행 | `python ask.py "질문"` |
| 확인 | 답변 + 참조 파일명 출력 |
| 산출물 | **이 시점에 RAG는 완성이다.** UI는 시연 포장일 뿐 |

프롬프트:
```
아래 문서 내용만 참고해서 답변하세요. 문서에 없는 내용은 "문서에서 찾을 수 없습니다"라고 답하세요.

[문서]
{검색된 청크 5개}

[질문]
{질문}
```

### 5단계 — Streamlit UI (시연용)

| 항목 | 내용 |
|---|---|
| 목표 | 고객 앞에서 보여줄 화면 |
| 실행 | `streamlit run app.py` |
| 확인 | 질문 입력창 / 답변 / 참조 파일명 3가지면 충분 |
| 주의 | **설정 사이드바 만들지 말 것.** 시연에서 안 쓰고 고장 지점만 늘어난다 |

### 6단계 — 전송 데이터 실측 ★ PoC의 결론

| 항목 | 내용 |
|---|---|
| 목표 | **외부로 나가는 내용을 정확히 특정한다** |
| 실행 | Gemini 호출 직전 전송 페이로드를 파일로 저장하도록 코드 한 줄 추가 |
| 확인 | 저장된 파일을 열어 실제 전송 내용 확인 |
| 산출물 | **"고객사에서 나가는 데이터는 이것이 전부"라는 증빙 자료** |

> 이전 완전 로컬 안에서는 "랜선 뽑고 시연"이 결론이었다.
> Gemini 구성에서는 **전송분을 눈으로 보여주는 것**이 그에 상응하는 설득 자료다.
> 고객이 "우리 문서가 통째로 나가는 거 아니냐"고 물을 때 파일로 답할 수 있다.

---

## 7. 실측 기록표

고객사 PC마다 채워서 제안 근거로 사용한다.

| 항목 | 개발 PC (2026-09-07) | 고객사 PC | 비고 |
|---|---|---|---|
| PC 사양 (CPU / RAM) | Windows 11 / Python 3.14.6 | | 임베딩이 로컬이므로 관련 있음 |
| 사내망 API 연결 | **성공** | | **0단계, 최대 리스크** |
| 답변 완료까지 | **3.22초** | | 목표 10초 → 달성 |
| 답변 첫 글자까지 | **0.86초** | | 체감 속도 지표 |
| 문서 N개 인덱싱 시간 | 분 | 전체 문서량 환산 근거 |
| 질문 5개 중 출처 정확 | / 5 | 목표 4 이상 |
| 사용 티어 | 무료 / 유료 | **실제 문서 사용 시 유료 필수** |
| 질문당 전송 데이터 크기 | KB | 고객 설명 자료 |

---

## 8. 미확인 항목 (실측으로 확정할 것)

- **고객사 프록시·방화벽 정책** — 0단계에서 확인. 도메인 허용 신청 시 수일 소요 가능.
- **Gemini 3.5 Flash 무료 티어 제한값** — AI Studio에서 직접 확인 (블로그 인용값 신뢰 금지).
- **LangChain 패키지 구성** — 설치 후 import 검증.
- **고객사 문서의 스캔 PDF 비율** — OCR 추가 여부 좌우. 1단계에서 확인.

---

## 9. 부록 — 확장 및 대안

### PoC 통과 후 확장 (지금은 만들지 않음)

| 확장 | 도입 조건 |
|---|---|
| 증분 인덱싱 | 문서가 수시로 바뀌고 전체 재인덱싱이 부담될 때 |
| BM25 하이브리드 검색 | 전문용어·품번 검색이 안 될 때 (**한국어는 형태소 분석기 필수**) |
| Cross-encoder 리랭킹 | Q3 정확도가 목표 미달일 때 |
| 사용자별 문서 권한 분리 | 부서별 접근 통제가 요구사항에 들어올 때 |

### 완전 폐쇄형 대안 (고객이 외부 전송을 일절 거부할 때)

LLM만 **Ollama 로컬 모델**로 교체하면 외부 전송이 0이 된다. 나머지 구조는 동일하다.

| 항목 | Gemini 구성 | Ollama 구성 |
|---|---|---|
| 외부 전송 | 질문 + 청크 5개 | **없음** |
| 답변 속도 | 빠름 | 느림 (일반 PC·CPU 기준) |
| 한국어 품질 | 우수 | 모델별 편차 큼 |
| 추가 반입 | 없음 | 모델 4~5GB |

> **금융·의료·방산 등 규제 산업 고객에게는 이 대안을 준비해 가는 것이 좋다.**
> 코드에서 LLM 호출부만 교체하면 되도록 `ask.py`를 작성해 둘 것.

---

**출처**: [Gemini API Terms of Service](https://ai.google.dev/gemini-api/terms) · [Gemini API Rate Limits](https://ai.google.dev/gemini-api/docs/rate-limits) · [Gemini API Pricing](https://ai.google.dev/gemini-api/docs/pricing)
